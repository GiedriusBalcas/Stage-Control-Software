radius = 100.0;
dtheta = 1;
speed = 1000;

xasPrev = 0;
yasPrev = radius
jumpA("xy",xasPrev,yasPrev);

for(theta = dtheta; theta < 2*PI; theta+=dtheta)
{
	xas = sin(theta)*radius;
	yas = cos(theta)*radius;
	jumpA("xy",xas,yas);
	xasPrev = xas;
	yasPrev = yas;
}

