

moveA.Speed = 100.01;
moveA.Line = true;

dl = 100;
radius = 100;
dtheta = asin(dl/radius);

for(theta = 0; theta <= 2*PI; theta += dtheta)
{
	xas = sin(theta)*radius;
	yas = cos(theta)*radius;
	zas = theta*10;
	moveA("xy", xas, yas);
}