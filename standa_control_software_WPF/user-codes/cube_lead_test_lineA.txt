width = 100;
length = 100;
height = 100;

kiekx = 10;
kiekz = 1;

set("s","DelayOn",-500);
set("s","DelayOff",1);

lineA.Speed = 1000.01;
lineA.LeadIn = true;
lineA.LeadOut = true;
lineA.Shutter = true;
set("xy","DefaultSpeed",1000);

y_prev = 0;
for( k=0; k<kiekz; k++)
{
	zas = height/kiekz*k;
	for( i=0; i<kiekx; i++)
	{
		direction = (-1)^i;
		
		xas = i*width/kiekx - width/2;
		yas = direction*length/2;
		
		lineA("xy", xas, y_prev, xas, yas);
		y_prev = yas;
	}
}
